db.collectionName.aggregate([
    {$match:
        {department: "CSE"}
    },
    {
        $group:{
                _id: "$department",
                Total: {$sum: "$salary"}
        }
    },

    {$project:{
        department: "$_id",
        Total: 1,
        _id: 0
        
    }}
])