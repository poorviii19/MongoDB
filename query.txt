db.collectionName.updateOne({email: "abc@gmail.com"},{$set: {Locations: ["bihar", "delhi"]}}) //it will update the Location set in collection

db.collectionName.updateOne({email: "abc@gmail.com"}, {$addToset: {Locations: ["agra", "raniganj", "bihar", "patna"] }})  // ..it will add unique only to the set to specified Locations (array inside array)

db.collectionName.updateOne({email: "abc@gmail.com"},{$pull: {Locations: ' '}})   //it removes specified thing from Location

db.collectionName.updateOne({email: "abc@gmail.com"},{$addToSet: {Locations: {$each: ["patna", "kolkata"]}}})  // (to avoid array inside array)

db.collectionName.updateOne({email: "abc@gmail.com""}, {$push: {Locations: {$each: ["vishakhapatnam", "gaya"]}}})  //similar as above but do not check uniqueness

db.collectionName.find({email: "abc@gmail.com"}).explain("executionStats")  //provides stats of data

db.collectionName.createIndex({email: 1})  //this will create a index in ascending order in all emails

db.collectionName.getIndexes() //to check whether index is created or not
//* this is use for finding fast data suppose a page number in book 
//* to check whether it is running or not   run this db.users.find({email:"ank@gmail.com"}).explain("executionStats")
 and check for "stage":"IXSCAN"  THEN IT IS WORKING FINE

 
